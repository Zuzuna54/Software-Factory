---
description: 
globs: **/*.ts", "**/*.tsx
alwaysApply: false
---
---
description: Next.js 14 + React 19 coding rules
globs: ["**/*.ts", "**/*.tsx"]
alwaysApply: false
---

- Use **server components** unless interaction is needed; client components must start with `"use client"`.
- Styling = Tailwind (`@apply` allowed only in `*.module.css`).
- UI primitives come from **shadcn/ui**; animate with **Framer Motion**.
- Fetch data through server actions wrapped by `getServerSession` for auth.
- Never mix `useState` and `useServerContext` in the same component.

@templates/react-component.tsx
