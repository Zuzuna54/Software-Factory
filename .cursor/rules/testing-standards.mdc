---
description: 
globs: **/tests/**", "**/*.test.*", "**/*.spec.*
alwaysApply: false
---
---
description: Test & QA expectations
globs: ["**/tests/**", "**/*.test.*", "**/*.spec.*"]
alwaysApply: false
---

- Python: pytest + pytest-asyncio; aim for deterministic tests (< 200 ms each).
- Frontend: Jest + @testing-library/react ; mock network with MSW.
- Each new PR must include at least one **happy‑path** and one **edge‑case** test.
- Snapshot tests allowed only for presentational components.
